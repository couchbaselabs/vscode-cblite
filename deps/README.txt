Extract the downloadable package into this folder and move everything out of the top level folder (e.g. mv libcblite-3.0.0/* .)

The CMake project will take care of downloading the C++ headers that are needed.